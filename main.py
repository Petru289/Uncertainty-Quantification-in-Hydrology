from histogramm import histogramm
from sensitivityAnalysis import sensitivityAnalysis

histogramm()

# The function expects the parameter you want to change as a string. Choose from: n, D, q, Lambda
sensitivityAnalysis('n')